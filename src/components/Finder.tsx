import styled from 'styled-components'
import {useState} from 'react'
import { player } from '../types'

const Finder = () =>{
  const playerArray = useState<player[]>([])
  const omitArray = useState<player[]>([])
  return (
    <>
    </>
  )
}


export default Finder